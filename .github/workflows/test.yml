name: Test Workflow

on:
  push:

jobs:
  test:
    runs-on: self-hosted
    steps:
      - run: echo "Hello Niranjan 🚀"

      - name: check hostname
        run: hostname

      - name: check current directory
        run: pwd 

      - name: check current user
        run: whoami
